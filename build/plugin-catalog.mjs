// Flowglass build catalog. Only the framework and Flowglass feature ship here.
export const PLUGINS = Object.freeze([
  {
    key: 'toolbox',
    idPrefix: 'tbx',
    order: 1,
    platform: 'host+client',
    approval: true,
    autoStart: true,
    name: 'Flowglass framework (Host registry and Client shell)',
    purpose: 'Provides the Host registry, Client UI shell, settings, and panel transport used by Flowglass.',
    inject: ['fs'],
    hostFiles: ['plugins/toolbox/host.js'],
    clientFile: 'plugins/toolbox/client.js',
    sharedHost: false,
    sharedRegistry: true,
    clientRpc: 'toolbox/client-impl',
    note: 'Internal framework for the standalone English Flowglass package.',
    bundle: { selectable: false, defaultLabel: 'Flowglass', aliases: [], dependencies: [], conflicts: [], scope: 'process' },
  },
  {
    key: 'flow',
    idPrefix: 'flow',
    order: 2,
    platform: 'host-only',
    approval: false,
    autoStart: true,
    name: 'Flowglass session visualization (Host-only)',
    purpose: 'Renders live session execution, tool calls, subagents, concurrent branches, history, and Flow Zoom views.',
    inject: ['fs', 'sessionQuery', 'timer'],
    hostFiles: ['plugins/flow/tool.js'],
    note: 'The standalone English-only Flowglass feature.',
    bundle: { selectable: true, defaultLabel: 'Flowglass', aliases: ['flow'], dependencies: [], conflicts: [], scope: 'workspace' },
  },
])
